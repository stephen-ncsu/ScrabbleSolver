<UserControl x:Class="ScrabbleUIWPF.RackControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="400">
    <UserControl.Resources>
        <!-- Normal Tile Style -->
        <Style x:Key="TileTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#F5DEB3"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#DAA520"/>
            <Setter Property="FontFamily" Value="Arial Black"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="MaxLength" Value="1"/>
            <Setter Property="CharacterCasing" Value="Upper"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="50"/>
        </Style>

        <!-- Unrecognized Tile Style -->
        <Style x:Key="UnrecognizedTileTextBox" TargetType="TextBox" BasedOn="{StaticResource TileTextBox}">
            <Setter Property="Background" Value="#FFC8C8"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="BorderBrush" Value="Red"/>
        </Style>
    </UserControl.Resources>
    
    <Border BorderBrush="Goldenrod" BorderThickness="2" Background="#FFFACD" Padding="10" CornerRadius="5">
        <StackPanel>
            <TextBlock Text="Your Rack" 
                       FontSize="16" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>
            <StackPanel x:Name="TilesPanel" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Center"/>
            <TextBlock Text="Use * for wildcard/blank tiles" 
                       FontSize="11"
                       Foreground="Gray"
                       HorizontalAlignment="Center"
                       Margin="0,5,0,0"/>
        </StackPanel>
    </Border>
</UserControl>
