<UserControl x:Class="ScrabbleUIWPF.ScrabbleBoardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600">
    <UserControl.Resources>
        <!-- Normal Square -->
        <Style x:Key="NormalSquare" TargetType="Border">
            <Setter Property="Background" Value="#DEB887"/>
            <Setter Property="BorderBrush" Value="#8B7355"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <!-- Double Letter Score -->
        <Style x:Key="DoubleLetterSquare" TargetType="Border">
            <Setter Property="Background" Value="#B0E0E6"/>
            <Setter Property="BorderBrush" Value="#4682B4"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <!-- Triple Letter Score -->
        <Style x:Key="TripleLetterSquare" TargetType="Border">
            <Setter Property="Background" Value="#4169E1"/>
            <Setter Property="BorderBrush" Value="#191970"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <!-- Double Word Score -->
        <Style x:Key="DoubleWordSquare" TargetType="Border">
            <Setter Property="Background" Value="#FFB6C1"/>
            <Setter Property="BorderBrush" Value="#FF69B4"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <!-- Triple Word Score -->
        <Style x:Key="TripleWordSquare" TargetType="Border">
            <Setter Property="Background" Value="#FF4500"/>
            <Setter Property="BorderBrush" Value="#DC143C"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <!-- Center Star Square -->
        <Style x:Key="CenterSquare" TargetType="Border">
            <Setter Property="Background" Value="#FFB6C1"/>
            <Setter Property="BorderBrush" Value="#FF69B4"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>
        
        <!-- Tile TextBox Style -->
        <Style x:Key="TileTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFACD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DAA520"/>
            <Setter Property="FontFamily" Value="Arial Black"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="MaxLength" Value="1"/>
            <Setter Property="CharacterCasing" Value="Upper"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="2"/>
        </Style>

        <!-- Highlighted Tile TextBox Style (for new moves) -->
        <Style x:Key="HighlightedTileTextBox" TargetType="TextBox" BasedOn="{StaticResource TileTextBox}">
            <Setter Property="Background" Value="#90EE90"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="BorderBrush" Value="#228B22"/>
        </Style>

        <!-- Empty Tile TextBox Style -->
        <Style x:Key="EmptyTileTextBox" TargetType="TextBox" BasedOn="{StaticResource TileTextBox}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        
        <!-- Header Label Style -->
        <Style x:Key="HeaderLabel" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Arial"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid x:Name="BoardGrid" Background="#F5F5DC" ShowGridLines="False">
                <!-- Will be populated in code-behind -->
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
